name: utopia_bytes
description: Typed binary serialization for Dart - ByteReader and ByteWriter with codecs for ints, floats and every endianness over ByteData.
topics:
  - serialization
  - bytes
  - binary
  - codec
  - encoding
version: 0.1.0
homepage: https://github.com/Utopia-USS/utopia-flutter

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  utopia_lints: ^0.0.5