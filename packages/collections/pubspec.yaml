name: utopia_collections
description: Extension methods and helpers for Dart collections - groupBy, distinctBy, sorting and multi-key comparators over Iterable, List, Set and String.
topics:
  - collections
  - extensions
  - dart
  - utils
  - iterable
version: 0.1.2
homepage: https://github.com/Utopia-USS/utopia-flutter

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  fast_immutable_collections: ^11.0.3

dev_dependencies:
  utopia_lints: ^0.0.5