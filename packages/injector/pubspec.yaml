name: utopia_injector
description: Lightweight dependency injection container for Dart and Flutter - register singletons, factories and instances, resolve by type, with keys and scoped injectors.
topics:
  - dependency-injection
  - di
  - service-locator
  - flutter
  - dart
version: 0.1.0
homepage: https://github.com/Utopia-USS/utopia-flutter

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  meta: ^1.10.0

dev_dependencies:
  utopia_lints: ^0.0.5