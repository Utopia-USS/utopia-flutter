name: utopia_rest
description: Utopia USS libs - REST API support with Dio
version: 0.1.0+4
homepage: https://github.com/Utopia-USS/utopia-flutter

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  utopia_utils: ^0.3.2
  utopia_reporter: ^0.1.0+4
  dio: ^5.0.0

dev_dependencies:
  utopia_lints: ^0.0.3+2